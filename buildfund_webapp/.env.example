# Example environment variables for BuildFund project
#
# Copy this file to `.env` and fill in the appropriate values for your
# environment.  These settings should never be committed to version
# control and should be kept secret in production.

# Django settings
DEBUG=False
SECRET_KEY=replace-me-with-a-secure-random-string
ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings.  Set DB_ENGINE to "djongo" to use MongoDB via
# Djongo.  When using Djongo, specify DB_NAME, DB_HOST, DB_PORT and
# optionally DB_USER and DB_PASSWORD.  For SQLite (development
# default), set DB_ENGINE to "django.db.backends.sqlite3" and DB_NAME
# to the file path.  Example for MongoDB:
#   DB_ENGINE=djongo
#   DB_NAME=buildfund_db
#   DB_HOST=localhost
#   DB_PORT=27017
DB_ENGINE=djongo
DB_NAME=buildfund_db
DB_HOST=localhost
DB_PORT=27017
DB_USER=
DB_PASSWORD=

# Third‑party API keys.  These keys are required for AI underwriting
# and map/address services.  Obtain these from the respective
# providers and store them securely.  Do not expose these keys in
# client‑side code.
OPENAI_API_KEY=your-openai-api-key-here
GOOGLE_API_KEY=your-google-api-key-here

# CORS: define allowed origins for API access.  Typically this will
# include the frontend domain.
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Rate limiting: customise throttling classes or leave default.
# REST_FRAMEWORK_RATELIMIT=100/hour