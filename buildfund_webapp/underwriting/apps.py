from django.apps import AppConfig


class UnderwritingConfig(AppConfig):
    """Configuration for the underwriting app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "underwriting"